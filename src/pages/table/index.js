import Layout from "../../components/layout";

const Table = ({location, number}) => {
    return(
        <Layout>
            <section>
                deneme
            </section>
        </Layout>
    )
}

export default Table;
